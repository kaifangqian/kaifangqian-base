<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.kaifangqian.modules.system.mapper.SysAuthGroupRoleMapper">

    <!-- 查询权限组下角色 -->
    <select id="pageExt" resultType="com.kaifangqian.modules.system.vo.SysAuthGroupRoleVO">
	    SELECT
            gr.id,
            gr.group_id,
            gr.role_id,
            sr.role_name
        FROM
            sys_auth_group_role gr
            INNER JOIN sys_role sr ON gr.role_id = sr.id
            AND sr.delete_flag = 0
        WHERE
            gr.group_id =  #{groupId, jdbcType=VARCHAR}
        ORDER BY
            sr.role_name
    </select>

</mapper>